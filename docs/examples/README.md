# Raw Source Examples

Copy these into your project's `wiki/raw/` when starting research. Do not commit example content into `raw/` — keep that directory for project-specific sources only.

| File | Copy to |
|------|---------|
| `raw-llm-chat.example.md` | `raw/llm/YYYY-MM-DD-<provider>-<topic>.md` |
| `raw-web-article.example.md` | `raw/web/<article-slug>.md` |

See `AGENTS.md` for frontmatter requirements.
