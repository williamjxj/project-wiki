- [[llm-web-api-context-token-management]] — 网页端大模型对话框自动累加全部聊天历史，每轮对话都带入全程所有问答，Token由平台默认管控，用户无法裁剪、压缩，上下文无限堆积，消耗不可控。

  - **Contrast with API Context Management**: While web-based models have a fixed, uncontrolled context that grows over time, APIs offer more flexible and controlled context management.
